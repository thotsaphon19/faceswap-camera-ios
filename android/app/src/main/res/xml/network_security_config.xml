<?xml version="1.0" encoding="utf-8"?>
<!--
  Release (and default) network security config.
  - Disables all cleartext (HTTP/WS) traffic for production security.
  - Trusts only system CAs in production.
  - The debug-overrides block additionally trusts user-installed CAs when
    running a debug build, but does NOT enable cleartext — that is handled
    by the separate src/debug/res/xml/network_security_config.xml which
    overrides this file entirely in debug builds.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
